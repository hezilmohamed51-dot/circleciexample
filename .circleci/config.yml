version: 2.1

jobs:
  hello-job:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: Say Hello
          


workflows:
  hello-workflow:
    jobs:
      - hello-job
